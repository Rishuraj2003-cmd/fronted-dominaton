// gsap.to("",{}) ---> used to move obj from left to right
// gsap.from("",{}) ---> used to move obj from right to left



gsap.from("#box",{
    x:1000,
    duration:2,
    delay:2,
    rotate:360,
    backgroundColor:"blue"
})